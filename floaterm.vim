" Configuration key map
let g:floaterm_keymap_new    = '<F36>'
let g:floaterm_keymap_prev   = '<F8>'
let g:floaterm_keymap_toggle = '<F12>'

" Configuratioin terminal window
let g:floaterm_width = 0.5              " Set width of floaterm equal to 50% view width
let g:floaterm_height = 0.5             " Set height of floaterm equal to 50% view height
let g:floaterm_position = 'topright'    " Set position of floaterm is topright
